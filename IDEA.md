إليك نسخة مصقولة ومُعادة الهيكلة باحترافية باللغة العربية لملف `README.md` الخاص بمشروع **Nodal**. تم التركيز على رفع الدقة التقنية، تحسين تدفق القراءة، وتنسيق العناصر بصرياً دون إخلال أو حذف لأي جزء من الهندسيات المذكورة.

---

# Nodal — شبكة اجتماعية لا مركزية (P2P)

> **المرجع المعماري الشامل**
> *آخر تحديث: أغسطس 2026*
> *حالة التنفيذ:* معتمد ومتوافق مع مكتبة `jvm-libp2p:1.3.5-RELEASE` (عبر Cloudsmith).
> *النسخة الإنجليزية:* [IDEA.en.md](https://www.google.com/search?q=./IDEA.en.md)

---

## 1. الرؤية والهدف

**Nodal** هي شبكة تواصل اجتماعي ومشاركة محتوى **لا مركزية بالكامل (Serverless)** ومصممة بنمط ند لند (Peer-to-Peer). تستوحي تجربة المستخدم وسرعة التفاعل من تويتر، لكن بدون وجود خادم مركزي يمتلك البيانات أو يتحكم في حركة الاتصال.

* **مقاومة الرقابة:** لا توجد سلطة مركزية تملك صلاحية الحجب أو الحذف.
* **الخصوصية والأمان:** اتصالات مشفرة افتراضياً، وبدون قاعدة بيانات مركزية عرضة للاختراق.
* **ملكية البيانات:** بياناتك مخزنة على جهازك ومع من تختار مشاركتها معهم فقط.
* **المرونة والاستمرارية:** تستمر الشبكة بالعمل بكفاءة حتى عند خروج العقد أو الانقطاع المفاجئ.

---

## 2. المبادئ الأساسية (غير قابلة للتفاوض)

* **غياب النسخة الرسمية:** لا وجود لقاعدة بيانات مركزية أو "مصدر وحيد للحقيقة" (Single Source of Truth).
* **الهوية = مفتاح تشفير (Ed25519):** توثيق البيانات يعتمد كلياً على التوقيع الرقمي دون الحاجة لسلطة إثبات خارجية.
* **كل جهاز هو عقدة كاملة (Client + Server):** يتوزع التخزين على أجهزة المستخدمين المهتمين بالمحتوى.
* **المحتوى منشورات مُعرّفة بنوع:** (Post / Profile / List / Like / Follow / Block) جميعها منشورات مُوقّعة.
* **حيادية البنية التحتية:** عقد الربط والترحيل (Bootstrap/Relay) تمرر البيانات فقط، ولا تملك سلطة التحكم أو التعديل.
* **النهج المحلي أولاً (Local-First):** التخزين المحلي هو كاش وأرشيف شخصي، وليس مصدر سلطة على الآخرين.

---

## 3. الهيكلية والمعمارية البرمجية

### الوحدات البرمجية (Modules)

| الوحدة | التقنية المستعملة | الدور المعماري |
| --- | --- | --- |
| **`core`** | Kotlin/JVM *(جاهزة لـ KMP)* | إدارة الهوية، توليد CIDs، التوقيع الرقمي، دورة حياة العقد، البروتوكولات، والموجز. |
| **`bootstrap`** | Kotlin/JVM | عقد البنية التحتية: اكتشاف (Rendezvous)، ترحيل (Relay)، ذاكرة حلقية، وفهرس اختياري. |
| **`android`** | Kotlin + Jetpack Compose | واجهة المستخدم بدعم كامل للاتجاه من اليمين لليسار (RTL): التايم لاين، الإنشاء، الملف الشخصي، والإعدادات. |

### طبقات النظام الثلاث (توزيع المسؤوليات)

```
┌────────────────────────────────────────────────────────────────────────┐
│                        طبقة ملكية البيانات                             │
│               جهاز المستخدم: Keystore + كاش محلي                      │
└──────────────────────────────────┬─────────────────────────────────────┘
                                   │
┌──────────────────────────────────▼─────────────────────────────────────┐
│                            طبقة الأقران                                │
│                GossipSub + تبادل الكتل + الاستعلام الموزع               │
└──────────────────────────────────┬─────────────────────────────────────┘
                                   │
┌──────────────────────────────────▼─────────────────────────────────────┐
│                         طبقة البنية التحتية                            │
│           عقد Rendezvous + Circuit Relay + ذاكرة حلقية مؤقتة           │
└────────────────────────────────────────────────────────────────────────┘

```

---

## 4. الهوية والتشفير وعنونة المحتوى

### نظام التشفير والهوية

* **المفاتيح:** توليد مفاتيح Ed25519 داخل **Android Keystore** (غير قابلة للاستخراج) مع دعم النسخ الاحتياطي المشفر.
* **معرف العقدة (PeerId):** حساب ذاتي عبر `multihash(identity)` للمفتاح العام المشفر بـ Protobuf بدون الحاجة لمرجع خارجي.
* **التوقيع الرقمي:** توقيع Ed25519 على **JSON قانوني (Canonical JSON)** (مفاتيح مرتبة أبجدياً، UTF-8، أرقام صحيحة).
* **التحقق المباشر:** استخراج المفتاح العام مباشرة من الـ PeerId معالجةً يدوياً لبنية Protobuf، ثم التثبت من التوقيع بدون أي طلب شبكي.
* **التشفير بين الطرفين (M5):** استخدام X25519 + ChaCha20-Poly1305 فوق طبقة Noise للمراسلات الخاصة فقط (المنشورات العامة مُوقّعة وليست مشفرة).

### عنونة المحتوى (CID من الصفر)

تم بناء نظام العنونة ذاتياً (~100 سطر برمجي) لدعم Base58btc و Base32 (RFC4648) و Multihash بدون مكتبات خارجية:

| النوع | البنية والترميز | الاستخدام |
| --- | --- | --- |
| **CIDv0** | `base58btc(0x12 0x20 sha256(data))` | معرّف المنشورات (تجزئة الـ JSON القانوني) |
| **CIDv1** | `raw base32(0x01 0x55 0x12 0x20 sha256(data))` | معرّف الوسائط والكتل البرمجية |

---

## 5. بروتوكولات الشبكة والاكتشاف (jvm-libp2p)

### مكدس البروتوكولات

* **النقل (Transport):** TCP
* **التشفير:** Noise (XX) إلزامي لكافة الاتصالات.
* **تجميع الاتصالات (Multiplexing):** mplex
* **النشر والاشتراك (PubSub):** GossipSub v1.x
* **بروتوكولات مساعدة:** Identify و Ping و mDNS للاكتشاف المحلي على شبكة الـ Wi-Fi.
* **الترحيل (Relay):** Circuit Relay عبر عقد الـ Bootstrap لمعالجة مشاكل الـ NAT.

### المواضيع (Topics) وقنوات البث

```
/nodal/u/{peer_id}   --> قناة المستخدم (المتابعة تعني الاشتراك)
/nodal/feed/1.0.0    --> قناة الاستكشاف العامة

```

> **قيود تقنية مُثبتة:**
> 1. عدم توفر Kademlia DHT في مكتبة `jvm-libp2p`: يعتمد الاكتشاف على الـ Bootstrap و mDNS والإضافة اليدوية والهوية المرفقة بالمحتوى.
> 2. غياب دعم WebRTC في المراحل (M1–M4): يتم الاعتماد على Relay لمتصلي NAT، مع التخطيط لإضافة WebRTC في المرحلة M6.
> 
> 

---

## 6. خط معالجة التايم لاين (Push + Pull)

### بروتوكولات Nodal الخاصة

تعتمد جميع البروتوكولات على تغليف موحد لـ Canonical JSON بطول بادئ (`4-byte big-endian length + UTF-8 payload`):

* `/nodal/blocks/1.0.0` — تبادل الكتل (طلب CID $\rightarrow$ إرجاع الكتلة أو NOT_FOUND).
* `/nodal/sync/1.0.0` — سحب التحديثات بعد مؤشر محدد `{ topics[], since }`.
* `/nodal/search/1.0.0` — البحث الموزع المحدد بمدى قفزات (TTL).
* `/nodal/dm/1.0.0` — المراسلات الخاصة المشفرة.

### آلية المعالجة المدمجة

```
          [ الفهرس المحلي ] ◄── دمج وإزالة التكرار ◄── [ ذاكرة الاستقبال ]
                                                               ▲
                                                               │
┌───────────────────────────────┐               ┌──────────────┴───────────────┐
│     السحب التعويضي (Pull)     │               │        البث المباشر (Push)     │
│  طلبات Sync لعقد الأقران      │               │   اشتراك GossipSub لمواضيع     │
│   والذاكرة الحلقية للـ Bootstrap │               │           المتابعين          │
└───────────────────────────────┘               └───────────────────────────────┘

```

1. **التحقق عند الاستقبال:** مطابقة التوقيع $\rightarrow$ التأكد من النافذة الزمنية ($\pm 5\text{ دقائق}$) $\rightarrow$ تطبيق حد الحجم الأقصى (64KB).
2. **الدمج والتنظيم:** إزالة التكرار اعتماداً على الـ CID، والترتيب الزمني التنازلي مع سقف ذاكرة يتسع لـ 500 منشور.

---

## 7. أنواع البيانات المعتمدة

| نوع المنشور (`type`) | الحقول الأساسية | الاستخدام |
| --- | --- | --- |
| `post` | `text`, `media[]` | المحتوى الأساسي (نصوص، صور، ملفات) |
| `profile` | `text` (سيرة), `media[0]` (رمز تعبيري) | بيانات الملف الشخصي المنشورة في قناة المستخدم |
| `list` | `payload` (قائمة تحتوي `entries[]`) | قوائم الحسابات أو المواضيع المنسقة والموقعة |
| `follow` | `ref` (PeerID الحساب المتابَع) | تمثيل علاقة المتابعة في الشبكة |
| `like` | `ref` (CID المنشور) | التفاعل والإعجاب القابل للعد |
| `block` | `payload` (قائمة تحتوي `entries[]`) | قائمة الحظر الشخصية لحماية المستخدم |

---

## 8. خارطة الطريق للتطوير

```
[M1] النواة الأساسية ──► [M2] شبكة الـ Bootstrap ──► [M3] واجهة الأندرويد ──► [M4] التخزين المحلي ──► [M5] التشفير والقوائم ──► [M6] WebRTC و KMP

```

* **M1: النواة والمزامنة الأولية** — الهوية، الـ CID، التوقيع، بروتوكولات المزامنة والكتل، واختبار محلي بين عقدتين.
* **M2: البنية التحتية الموسعة** — تشغيل الذاكرة الحلقية، الـ Relay، الـ Rendezvous، واختبار الاتصال عبر الإنترنت الحقيقي.
* **M3: تطبيق الأندرويد** — تطوير الواجهات بـ Jetpack Compose ودعم واجهة RTL الكاملة.
* **M4: الكاش والتصفح دون اتصال** — دمج قاعدة بيانات Room المحلية وتنسيق سياسات التحميل عبر Wi-Fi.
* **M5: الخصوصية والتنسيق** — إطلاق المراسلات المشفرة (E2EE)، الواجهات الخاصة بالمجتمعات وقوائم التوصية.
* **M6: دعم المنصات المتقدم** — تفعيل WebRTC ونقل النواة لتخدم Kotlin Multiplatform (iOS و Web).

---

## 9. المقايضات المعمارية والقيود المقبولة

| الميزة | القيد المعماري | التكيّف المعماري المعتمد |
| --- | --- | --- |
| **البحث** | نطاق بحث محلي (Bubble Search) | البحث عبر استعلامات محددة بقفزات (TTL)؛ لا يوجد كشاف كوني. |
| **المحتوى الشائع (Trending)** | رائج محلي نسبي | تجميع وتعديد التفاعلات من الأقران المتصلين بدلاً من خوارزمية فرز مركزية. |
| **وفرة البيانات** | متوقفة على التكرار (Replication) | غياب العقدة المنشورة وعدم تكرار محتواها يجعل المحتوى غير متاح لحين عودتها. |
| **التشغيل الأول** | الاعتماد على جودة الـ Bootstrap | تضمين عناوين متعددة ومفتوحة المصدر لـ Bootstrap داخل التطبيق مع إمكانية تعديلها. |

---

## 10. معايير النجاح النهائي

1. نجاح نقل البيانات والتحقق منها بين جهازين عبر **إنترنت حقيقي** (بدون شبكة محلية أو Localhost).
2. عرض خط التايم لاين لمنشورات موقعة ومتحقق من سلامتها التشفيرية بنجاح.
3. استمرارية عمل الشبكة بكفاءة حتى في حال إيقاف عقد الـ Bootstrap الأساسية (في حال وجود أقران متصلين).
4. انعدام وجود أي نقطة تحكم أو فشل أحادية (No Single Point of Failure/Control).
