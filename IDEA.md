# Nodal — شبكة اجتماعية لا مركزية (P2P)

> **المرجع المعماري الكامل** — آخر تحديث: أغسطس 2026
> كل قرار هنا مبني على فحص فعلي لمكتبة `jvm-libp2p 1.3.5-RELEASE` (أحدث إصدار، Cloudsmith)
> النسخة الإنجليزية: [IDEA.en.md](IDEA.en.md)

---

## 1. الرؤية

شبكة تواصل اجتماعي ومشاركة محتوى **لا مركزية بالكامل**: لا خادم مركزي يملك البيانات أو يتحكم بالاتصالات.
كل جهاز (هاتف/كمبيوتر) هو عقدة تعمل كعميل وخادم معًا. مستوحاة من تويتر في البساطة وسرعة التفاعل.

- **مقاومة الرقابة**: لا سلطة مركزية تحذف أو تحجب المحتوى
- **الخصوصية والأمان**: لا قاعدة بيانات مركزية تُخترق، والاتصالات مشفرة دائمًا
- **ملكية المستخدم**: بياناتك عندك وعند من تختار مشاركتها معه
- **المرونة**: استمرار الخدمة حتى لو خرجت عقد من الشبكة

---

## 2. المبادئ الأساسية (غير قابلة للتفاوض)

1. **لا توجد نسخة رسمية من أي شيء** — لا قاعدة بيانات مركزية، لا "سجل الحقيقة"
2. **الهوية = مفتاح** (Ed25519) — البيانات تُوقَّع لا تُوثَّق بسلطة
3. **كل جهاز عقدة** (عميل + خادم) — التخزين يُوزَّع على من يهتم بالمحتوى
4. **كل شيء = منشور موقّع بنوع محدد** — post / profile / list / like / follow / block
5. **البنية التحتية موجودة لكنها محايدة** — عقد bootstrap/relay تمرر ولا تتحكم ولا تحذف
6. **محلي-أول (Local-First)** — التخزين المحلي كاش وأرشيف، لا سلطة

---

## 3. البنية المعمارية

### الوحدات
| الوحدة | التقنية | الدور |
|---|---|---|
| `core` | Kotlin/JVM (جاهزة لـ KMP لاحقًا) | الهوية، CID، التوقيع، العقدة، البروتوكولات، الموجز |
| `bootstrap` | Kotlin/JVM | عقدة البنية التحتية: ربط + relay + ذاكرة حلقية + فهرس اختياري |
| `android` | Kotlin + Jetpack Compose (RTL عربي) | الواجهة: تايم لاين / إنشاء / ملف / أشخاص / إعدادات |

### الطبقات الثلاث (توزيع الوظائف — لا توجد طبقة تملك كل شيء)
| الطبقة | الدور | ملكية البيانات |
|---|---|---|
| **جهاز المستخدم** | Keystore + كاش محلي + ذاكرة حية | مفتاحه + أرشيف ما رآه |
| **الأقران** | GossipSub + تبادل كتل + استعلامات موزعة | نسخ مما يهمهم |
| **bootstrap** | ربط (rendezvous) + relay + ذاكرة حلقية مؤقتة + فهرس عناوين اختياري | لا يملك شيئًا دائمًا — ذاكرة مؤقتة ضحلة |

---

## 4. الهوية والتشفير

- **المفتاح**: Ed25519 — يُولَّد داخل **Android Keystore** على أندرويد (لا يُستخرج أبدًا) + نسخة احتياطية مشفرة بكلمة مرور
- **المعرف (PeerId)**: multihash(identity) لتمثيل protobuf للمفتاح العام — محسوب ذاتيًا من المفتاح، بلا أي سلطة
- **التوقيع**: Ed25519 فوق **JSON قانوني** (CanonicalJson: مفاتيح مرتبة أبجديًا، UTF-8، أرقام بدون كسور شاذة)
- **التحقق**: استخراج المفتاح العام من الـ PeerId (تحليل protobuf يدوي) ثم `verify` — بلا استدعاء أي خدمة
- **E2EE (M5)**: X25519 + ChaCha20-Poly1305 عبر طبقة Noise الموجودة — للمراسلة الخاصة فقط، المنشورات العامة موقعة غير مشفرة

---

## 5. عنونة المحتوى (CID) — من الصفر

| النوع | التركيب | الاستخدام |
|---|---|---|
| CIDv0 | base58btc(0x12 0x20 sha256(بيانات)) | معرف المنشورات (تجزئة JSON القانوني) |
| CIDv1 raw | base32(0x01 0x55 0x12 0x20 sha256(بيانات)) | معرف الوسائط والكتل |

- التنفيذ من الصفر: Base58btc + Base32 (RFC4648) + Multihash (~100 سطر) — بدون مكتبات خارجية
- CID ذاتي (self-describing): أي منشور = تجزئة محتواه → **إزالة تكرار طبيعية** وتحقّق تكامل تلقائي

---

## 6. الشبكة (libp2p)

### المكدس
- **Transport**: TCP
- **التشفير**: Noise (XX) — إلزامي لكل اتصال
- **Multiplexing**: mplex
- **PubSub**: GossipSub v1.x
- **بروتوكولات مساعدة**: Identify + Ping
- **mDNS**: اكتشاف تلقائي على نفس شبكة الواي فاي
- **Relay**: circuit relay عبر bootstrap (لأقران NAT) — beta في jvm-libp2p، يُختبر فعليًا

### المواضيع
| الموضوع | الاستخدام |
|---|---|
| `/nodal/u/<peerId>` | موضوع المستخدم — متابعة = اشتراك |
| `/nodal/feed/1.0.0` | الموجز العام — اكتشاف |

### القيود المعروفة (موثقة من فحص المكتبة)
- **لا Kademlia DHT في jvm-libp2p** → الاكتشاف: bootstrap + mDNS + إضافة يدوية + من المحتوى
- لا WebRTC → أقران NAT عبر relay فقط (M1–M4)، WebRTC في M6

---

## 7. التايم لاين — Push + Pull + فهرس محلي

```
push: اشتراك GossipSub في مواضيع المتابَعين → يصل فورًا
pull: عند الفتح → /nodal/sync/1.0.0 "كل ما بعد مؤشري" من bootstrap + الأقران
دمج: إزالة تكرار بـ CID → ترتيب زمني تنازلي → حد 500
تعويض ما فات: الذاكرة الحلقية عند bootstrap (آخر 500 منشور لكل موضوع نشط)
```

- كل منشور وارد يُتحقق منه (توقيع + نافذة زمنية ±5 دقائق + حد حجم 64KB) قبل قبوله
- `seq` يُستخدم للترتيب الداخلي والعدّادات فقط — **لا يوجد فحص تصاعدي صارم له بعد**: مسار sync (السحب التعويضي) يجلب التاريخ كاملًا، وفحص صارم سيرفض منشورات قديمة-seq تصل متأخرة. (التحقق الصارم بالـ seq يتطلب مؤشر (seq, createdAt) لكل مؤلف — مؤجل لـ M2 مع الكاش المحلي.)
- المتابعون = مجموعة PeerId في الذاكرة/الكاش، تُبنى من منشورات `follow` المرصودة ومن الإضافة اليدوية

---

## 8. اكتشاف الأقران وإضافة الأشخاص (5 طرق)

1. **bootstrap مدمج**: قائمة عناوين في التطبيق تُوصَّل عند أول تشغيل
2. **mDNS**: على نفس الشبكة المحلية تلقائيًا
3. **يدوي**: لصق/مسح `multiaddr + PeerId`
4. **من المحتوى**: أي منشور يصل يحمل هوية كاتبه → زر "متابعة" يضيفه فورًا
5. **قوائم موقّعة (list)**: منشور يحمل معرفات مجموعة → متابعة الجميع بنقرة (الطريقة الأساسية للاكتشاف)

---

## 9. البحث عن الأعضاء — ثلاث طبقات متدرجة

1. **محلي**: في كاش العقدة (متابعون، من راسلوك، ما وصل عبر pubsub) — فوري بلا شبكة
2. **موزّع**: `/nodal/search/1.0.0` — استعلام لأقران متصلين بحد قفزات TTL، كل عقدة تبحث محليًا وترد، تدمج النتائج (تقنية Gnutella)
3. **فهرس اختياري**: bootstrap يقدم فهرس **عناوين فقط** (اسم/معرف/صورة CID — بلا محتوى) — اختياري وقابل للاستبدال كاختيار مزود DNS

**الحد الصادق**: بحث "فقاعة الشبكة" لا بحث كوني — مقايضة مقبولة بلا فهرس مركزي.

---

## 10. الشهرة والرائج — بلا خوارزمية مركزية

| الآلية | الدور |
|---|---|
| **قوائم منسّقة موقّعة** | الأساس: "مشاهير التقنية" = منشور list؛ الثقة تنتقل عبر الشبكة الاجتماعية |
| **حسابات بذرة** | مجموعة حسابات في أول تشغيل، محتواها مكرَّر عند عقد كثيرة = متاح دائمًا |
| **عدّادات محلية** | كل عقدة تحسب الإعجابات/إعادة النشر التي رأتها؛ "الرائج" = دمج ردود الأقران المتصلين |

الشهرة = من يتابعه الكثيرون ويتكرر محتواه — لا من ترفعه خوارزمية.

---

## 11. بروتوكولات Nodal (إصدارات)

| البروتوكول | الغرض | التفاصيل | المرحلة |
|---|---|---|---|
| `/nodal/blocks/1.0.0` | تبادل كتل (IPFS-style) | طلب: CID → رد: كتلة أو NOT_FOUND | M1 |
| `/nodal/sync/1.0.0` | سحب المنشورات منذ مؤشر | طلب: {topics[], since} → رد: {posts[]} | M1 |
| `/nodal/search/1.0.0` | استعلام موزع | طلب: {query, ttl} → رد: {results[]} | M2 |
| `/nodal/dm/1.0.0` | مراسلة خاصة E2EE | مفتاح X25519 + ChaCha20-Poly1305 | M5 |

**التغليف الموحد**: كل بروتوكول = JSON قانوني بترميز طول-بادئ (4 بايت طول + حمولة UTF-8).

---

## 12. أنواع المنشورات

| type | الحقول المهمة | الاستخدام |
|---|---|---|
| `post` | text, media[] | المحتوى الأساسي (نص/صور/ملفات) |
| `profile` | text (سيرة), media[0]=الصورة | الملف الشخصي — يُنشر في موضوعك |
| `list` | payload (JSON: entries[]) | قوائم مشاهير/مواضيع موقعة |
| `follow` | ref = PeerId المتابَع | شبكة العلاقات مفتوحة وقابلة للمزامنة |
| `like` | ref = CID المنشور | إعجاب قابل للعد |
| `block` | payload (JSON: entries[]) | قائمة حظر شخصية — حق المستخدم |

---

## 13. الأدوار والأمان

- **bootstrap لا يوقّع ولا يعدّل ولا يحذف** — يمرر فقط. المستخدم مسؤول قانونيًا عن محتواه (لا وسيط)
- **تحقق إلزامي عند الاستقبال**: التوقيع → نافذة زمنية → حد حجم 64KB (pubsub والمزامنة معًا)
- **حماية من الإغراق**: حد حجم كتلة 10MB (مطبق) + حد معدل نشر لكل مؤلف (M2 — يتطلب مخزنًا دائمًا)
- **الاعتدال**: قوائم حظر موقعة + مجتمعات بمواضيع خاصة (اشتراك = قبول القواعد)
- **ضغط**: gzip اختياري للكتل الكبيرة في M4

---

## 14. البطارية والبيانات

- اتصال نشط فقط أثناء فتح التطبيق — **لا خدمة خلفية دائمة** (M1–M3)
- وسائط كبيرة: سحب على Wi-Fi فقط
- مؤشر زمني (cursor) لكل قناة — لا مزامنة كاملة أبدًا

---

## 15. التخزين

| المرحلة | التخزين | السبب |
|---|---|---|
| M1–M3 | **ذاكرة فقط + ملف مفتاح** (لا قاعدة بيانات!) | إثبات النموذج — "نهر حي" كما في المفهوم |
| M4 | كاش محلي Room (أرشيف القراءة دون اتصال) | **كاش لا سلطة** — لا توجد نسخة رسمية |

---

## 16. خارطة الطريق

| المرحلة | المحتوى | معيار النجاح |
|---|---|---|
| **M1** | core: هوية + CID + توقيع + عقدة + blocks + sync + موجز في الذاكرة + اختبار عقدتين محليًا | منشور موقّع ينتقل من عقدة لأخرى ويُتحقق |
| **M2** | bootstrap كاملة: ذاكرة حلقية + relay + rendezvous + فهرس عناوين | عقدتان عبر إنترنت حقيقي (لا localhost) |
| **M3** | أندرويد Compose RTL: تايم لاين / إنشاء / ملف / أشخاص / إعدادات | APK يعمل على جهازين |
| **M4** | كاش محلي + قراءة دون اتصال + Wi-Fi-only للوسائط | تمرير التاريخ دون اتصال |
| **M5** | E2EE + مجتمعات + واجهة قوائم | رسالة خاصة غير مقروءة من bootstrap |
| **M6** | WebRTC + نقل KMP (iOS/ويب) | نفس النواة على منصتين |

---

## 17. القيود الصادقة (مقايضات مقبولة — لا نخفيها)

- ❌ بحث **فقاعة** لا كوني (بلا فهرس مركزي مستحيل)
- ❌ رائج **محلي نسبي** لا كوني
- ❌ توفر = تكرار — لا ضمانات (العقدة غير المنسوخة تختفي)
- ❌ لا أرشيف كوني — التاريخ ينتهي عند حدود النسخ
- ⚠️ نجاح أول تجربة يعتمد على جودة bootstrap (اختياري ومتعدد ومفتوح المصدر)

---

## 18. تعريف النجاح النهائي

1. يعمل على جهازين عبر **إنترنت حقيقي** (لا localhost)
2. تايم لاين يعرض منشورات **موقعة ومتحققًا منها**
3. إيقاف أي bootstrap لا يقتل الشبكة (إن وُجد بديل)
4. لا توجد أي نقطة تحكم وحيدة — لا خادم، لا مزود، لا سلطة
